package Repositories;

import Models.ParkingLot;

public class ParkingLotRepository {
    public ParkingLot getParkingLot(Long gateId) {
        //First fetch all the parkingLots in the DB.
        //then check which parking has the gate with the gate id passed in the input.
        return null;
    }
}
